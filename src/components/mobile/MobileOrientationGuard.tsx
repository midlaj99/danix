import React from 'react';

/**
 * MobileOrientationGuard:
 * In DANIX Mobile Update, Portrait mode is a fully supported first-class citizen!
 * No blocking modals or rotation demands.
 */
export const MobileOrientationGuard: React.FC = () => {
  return null;
};
